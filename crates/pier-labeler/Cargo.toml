[package]
name = "pier-labeler"
description = "The KOTVA `labeler` coordinator kind: moderation labels over public objects, opt-in and subscribable — the canonical CONTRACT §4 derived-view carve-out example."
publish.workspace = true
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[lints]
workspace = true

[dependencies]
pier-economics = { path = "../pier-economics" }
pier-conformance = { path = "../pier-conformance" }
